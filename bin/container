#!/usr/bin/env bash
set -Euo pipefail

case $1 in
  up)
    while true; do
      docker-compose up -t 0 --build --force-recreate || true
      sleep 0.1
    done
  ;;
  shell)
    while true; do
      docker-compose exec app bash || true
      sleep 1
    done
  ;;
esac
